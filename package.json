{
  "name": "bn-automobiles",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.9.4",
  "scripts": {
    "dev": "turbo dev --filter=web",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier --write ."
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.8.12"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
